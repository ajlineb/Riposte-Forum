<div>
    <div class='container px-5 py-24 mx-auto flex flex-wrap items-center'>
        
        
            <div class="relative flex flex-col w-80 mb-5">
                <label for="create-post" class="leading-7 text-sm text-gray-600">Create New Post</label>
                <input type="text" id="create-post" name="create-post" placeholder="Create Post" class="w-full bg-gray-100 bg-opacity-50 rounded border border-gray-300 focus:border-indigo-500 focus:bg-transparent focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
            </div>
            <div class="w-full"></div>
            <!--post-->
            {{#each posts as |post|}}
            {{> posts-block}}
            {{/each}}
    </div>
    <div class='w-full'></div>
    <!--post-->
<script src="/js/comment.js"></script>